{
  "name": "studybuddy-core",
  "version": "1.0.0",
  "description": "StudyBuddy - Smart Study Management App",
  "scripts": {
    "build": "cd src/frontend && npm ci --only=production && npm run build",
    "build:backend": "cd src/backend && npm ci --only=production && npx prisma generate && npm run build",
    "dev": "echo 'Use npm run dev in frontend or backend directories'",
    "start": "echo 'This is a full-stack app. Frontend is static, backend runs on Vercel Functions.'"
  },
  "workspaces": [
    "src/frontend",
    "src/backend"
  ],
  "private": true,
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "cors": "^2.8.5",
    "date-fns": "^4.1.0",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "googleapis": "^159.0.0",
    "openai": "^5.20.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.21",
    "@types/node": "20.6.2",
    "prisma": "6.7.0",
    "typescript": "5.2.2"
  }
}